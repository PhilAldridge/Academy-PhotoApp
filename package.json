{
  "name": "hack-day-1-boilerplate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && cd .output/public && aws s3 sync . s3://BUCKET_NAME --profile default"
  },
  "devDependencies": {
    "nuxt": "^3.0.0"
  }
}
